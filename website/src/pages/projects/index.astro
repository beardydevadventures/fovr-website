---
import Layout from '@/layouts/PageLayout.astro';
import Image from '@/components/common/Image.astro';
import { Icon } from 'astro-icon/components';
import { getCollection } from 'astro:content';

const projectEntries = await getCollection('projects');
const projects = projectEntries
  .map(({ slug, data }) => ({
    slug,
    ...data,
  }))
  .sort((a, b) => {
    const orderA = a.order ?? 999;
    const orderB = b.order ?? 999;
    return orderA - orderB || a.title.localeCompare(b.title);
  });

const shippedCount = projects.filter((project) => project.status?.toLowerCase().includes('live')).length;

const metadata = {
  title: 'Projects',
  description:
    'Browse FoVR Interactive projects across released VR experiences, Meta Quest games, immersive learning, WebXR, mixed reality and FoVR Labs prototypes.',
};
---

<Layout metadata={metadata}>
  <section class="portfolio-hero relative isolate overflow-hidden px-4 py-20 sm:px-6 lg:py-28">
    <div class="portfolio-grid-bg" aria-hidden="true"></div>
    <div class="mx-auto max-w-6xl">
      <p class="portfolio-kicker">FoVR Portfolio</p>
      <h1 class="mt-5 max-w-4xl text-5xl font-black tracking-tight text-slate-950 dark:text-white sm:text-6xl lg:text-7xl">
        Released VR experiences and FoVR Labs experiments.
      </h1>
      <p class="mt-6 max-w-3xl text-lg leading-8 text-slate-700 dark:text-slate-300 sm:text-xl">
        A mix of playable Quest and SteamVR projects, immersive learning work, and smaller lab builds exploring mixed reality, WebXR, AI and spatial UI.
      </p>

      <div class="mt-8 grid gap-4 sm:grid-cols-3">
        <div class="portfolio-stat">
          <strong>{projects.length}</strong>
          <span>portfolio projects</span>
        </div>
        <div class="portfolio-stat">
          <strong>{shippedCount}</strong>
          <span>live experiences</span>
        </div>
        <div class="portfolio-stat">
          <strong>Quest</strong>
          <span>primary XR platform</span>
        </div>
      </div>
    </div>
  </section>

  <section class="relative px-4 py-16 sm:px-6 sm:py-24">
    <div class="mx-auto max-w-6xl">
      <div class="mb-10 flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between">
        <div>
          <p class="portfolio-kicker portfolio-kicker-dark">Project collection</p>
          <h2 class="mt-3 text-4xl font-black tracking-tight text-slate-950 dark:text-white sm:text-5xl">
            Shipped work, prototypes and research in one place.
          </h2>
          <p class="mt-4 max-w-2xl text-lg leading-8 text-slate-600 dark:text-slate-300">
            Live projects link through to their store pages. Lab projects show the experiments behind FoVR’s next ideas.
          </p>
        </div>
        <div class="portfolio-filter-row" aria-label="Project categories">
          <span>VR Games</span>
          <span>Immersive Learning</span>
          <span>Labs</span>
        </div>
      </div>

      <div class="portfolio-grid">
        {projects.map((project) => (
          <article class="portfolio-card group">
            <a class="absolute inset-0 z-20" href={`/projects/${project.slug}`} aria-label={`Open ${project.title}`} />

            <div class="portfolio-media">
              <Image
                class="h-full w-full object-contain p-6 transition duration-700 group-hover:scale-[1.04]"
                src={project.cover.src}
                alt={project.cover.alt}
                width={1280}
                height={720}
                loading="lazy"
              />
            </div>

            <div class="portfolio-content">
              <div class="mb-3 flex flex-wrap items-center gap-2">
                <span class="portfolio-label">{project.category ?? 'XR Project'}</span>
                {project.status && <span class="portfolio-status">{project.status}</span>}
              </div>

              <h2>
                <a href={`/projects/${project.slug}`}>{project.title}</a>
              </h2>

              {project.tagline && <p class="portfolio-tagline">{project.tagline}</p>}
              <p class="portfolio-excerpt">{project.excerpt}</p>

              {project.tags?.length ? (
                <div class="mt-5 flex flex-wrap gap-2">
                  {project.tags.map((tag) => <span class="portfolio-chip">{tag}</span>)}
                </div>
              ) : ''}

              <div class="relative z-30 mt-6 flex flex-wrap gap-3">
                <a class="portfolio-button portfolio-button-primary" href={`/projects/${project.slug}`}>View project</a>
                {project.links?.map((link) => (
                  <a
                    class="portfolio-button portfolio-button-secondary"
                    href={link.href}
                    {...link.external ? { target: '_blank', rel: 'noreferrer' } : {}}
                  >
                    {link.icon && <Icon name={link.icon} class="h-4 w-4" aria-hidden="true" />}
                    {link.label}
                  </a>
                ))}
              </div>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>
</Layout>

<style is:inline>
  .portfolio-hero {
    background:
      radial-gradient(circle at 20% 18%, rgba(34, 211, 238, 0.18), transparent 32rem),
      radial-gradient(circle at 82% 10%, rgba(236, 72, 153, 0.15), transparent 30rem),
      linear-gradient(135deg, #f8fafc, #eff6ff 55%, #ffffff);
  }

  .dark .portfolio-hero {
    background:
      radial-gradient(circle at 20% 18%, rgba(34, 211, 238, 0.2), transparent 32rem),
      radial-gradient(circle at 82% 10%, rgba(236, 72, 153, 0.19), transparent 30rem),
      linear-gradient(135deg, #020617, #08111f 55%, #111827);
  }

  .portfolio-grid-bg {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(15, 23, 42, 0.06) 1px, transparent 1px),
      linear-gradient(90deg, rgba(15, 23, 42, 0.06) 1px, transparent 1px);
    background-size: 54px 54px;
    mask-image: linear-gradient(to bottom, black, transparent 78%);
    opacity: 0.72;
  }

  .dark .portfolio-grid-bg {
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.055) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.055) 1px, transparent 1px);
  }

  .portfolio-kicker {
    margin: 0;
    color: #0891b2;
    font-size: 0.78rem;
    font-weight: 900;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .dark .portfolio-kicker {
    color: #22d3ee;
  }

  .portfolio-kicker-dark {
    color: #0891b2;
  }

  .portfolio-stat {
    border: 1px solid rgba(148, 163, 184, 0.22);
    border-radius: 1.4rem;
    background: rgba(255, 255, 255, 0.76);
    padding: 1rem;
    backdrop-filter: blur(18px);
    box-shadow: 0 18px 55px rgba(15, 23, 42, 0.08);
  }

  .dark .portfolio-stat {
    border-color: rgba(255, 255, 255, 0.12);
    background: rgba(255, 255, 255, 0.08);
    box-shadow: none;
  }

  .portfolio-stat strong,
  .portfolio-stat span {
    display: block;
  }

  .portfolio-stat strong {
    color: #0f172a;
    font-size: 1.8rem;
    font-weight: 950;
    line-height: 1;
  }

  .dark .portfolio-stat strong {
    color: white;
  }

  .portfolio-stat span {
    margin-top: 0.35rem;
    color: #475569;
    font-weight: 750;
  }

  .dark .portfolio-stat span {
    color: #cbd5e1;
  }

  .portfolio-filter-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem;
  }

  .portfolio-filter-row span {
    border-radius: 999px;
    border: 1px solid rgba(15, 23, 42, 0.08);
    background: rgba(15, 23, 42, 0.06);
    color: #0f172a;
    padding: 0.55rem 0.85rem;
    font-size: 0.8rem;
    font-weight: 850;
  }

  .dark .portfolio-filter-row span {
    border-color: rgba(255, 255, 255, 0.1);
    background: rgba(255, 255, 255, 0.08);
    color: #e2e8f0;
  }

  .portfolio-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1.4rem;
  }

  .portfolio-card {
    position: relative;
    overflow: hidden;
    border: 1px solid rgba(148, 163, 184, 0.22);
    border-radius: 2rem;
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.96), rgba(248, 250, 252, 0.9));
    box-shadow: 0 24px 80px rgba(15, 23, 42, 0.1);
    transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease;
  }

  .dark .portfolio-card {
    border-color: rgba(255, 255, 255, 0.11);
    background: linear-gradient(180deg, rgba(15, 23, 42, 0.98), rgba(2, 6, 23, 0.98));
    box-shadow: 0 24px 80px rgba(0, 0, 0, 0.35);
  }
</style>
